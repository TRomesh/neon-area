{
  "name": "neon-area",
  "version": "0.1.0",
  "description": "node module for finding area",
  "main": "index.node",
  "scripts": {
    "build": "cargo-cp-artifact -nc index.node -- cargo build --message-format=json-render-diagnostics",
    "build-debug": "npm run build --",
    "build-release": "npm run build -- --release",
    "install": "npm run build-release",
    "test": "cargo test"
  },
  "author": "Tharaka Romesh <tharakaromesh@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "cargo-cp-artifact": "^0.1"
  },
  "keywords": [
    "Rust",
    "Nodejs",
    "neon-bindings"
  ]
}
